import { expect } from 'chai';
import { countingSundays } from './counting-sundays';

describe('countingSundays', () => {
    it('should return 1', () => {
        // expect(countingSundays()).equal(1);
    });
});
