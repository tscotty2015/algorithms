import { expect } from 'chai';
import { longestCommonPrefix } from './longest-common-prefix';

describe('longestCommonPrefix', () => {
    it('should return 1', () => {
        // expect(longestCommonPrefix()).equal(1);
    });
});
