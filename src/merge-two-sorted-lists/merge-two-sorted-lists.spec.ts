import { expect } from 'chai';
import { mergeTwoSortedLists } from './merge-two-sorted-lists';

describe('mergeTwoSortedLists', () => {
    it('should return 1', () => {
        // expect(mergeTwoSortedLists()).equal(1);
    });
});
