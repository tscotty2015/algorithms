import { expect } from 'chai';
import { validParentheses } from './valid-parentheses';

describe('isPrime', () => {
  it('should return true for 2', () => {
    expect(validParentheses('')).equal(true);
  });
});